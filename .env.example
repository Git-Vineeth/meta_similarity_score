# Copy to .env and fill in. Without a token the app runs on mock creatives.
# Token needs the `ads_read` permission. The meta-ads MCP is interactive-only and
# cannot be used from this standalone script — use a Marketing API System User token.
META_ACCESS_TOKEN=
META_API_VERSION=v21.0
